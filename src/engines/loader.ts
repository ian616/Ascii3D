import { useEffect } from "react";
import type { Polygon } from "../entities/polygon";
import { create, all, Matrix } from 'mathjs';

export default function useLoader() {
    const math = create(all);

    const parseObjtoPolygons = ()=>{

    };
}
